// The admin console was intentionally removed from the project per repository owner request.
// If you need to restore it later, re-add an admin HTTP server (e.g., using express) and
// provide ADMIN_USER / ADMIN_PASS environment variables for basic auth.

module.exports = {};
